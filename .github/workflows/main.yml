name: remote ssh command
on: [push]
jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: executing remote ssh commands using password
      uses: appleboy/ssh-action@master
      with:
        host: '35.188.172.48'
        username: luisjcenteno17
        port: ${{ secrets.SSH_KEY }}
        script: whoami
